services:
  - type: web
    name: taskflow
    env: node
    region: oregon
    plan: free
    buildCommand: "npm install"
    startCommand: "node backend/server.js"
    envVars:
      - key: PORT
        value: 10000